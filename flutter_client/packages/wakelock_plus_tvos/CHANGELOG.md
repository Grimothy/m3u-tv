## 0.0.2

* Re-verified against `wakelock_plus` 1.8.0 (previously 1.7.0). No native code
  changes - regenerated Pigeon messages only (equality/hash/description
  helpers from the newer Pigeon codegen version).

## 0.0.1

* Initial tvOS scaffolding generated by `flutter-tvos plugin port` from
  `wakelock_plus`.
